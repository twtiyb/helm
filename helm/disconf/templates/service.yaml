apiVersion: v1
kind: Service
metadata:
  name: {{ include "disconf.fullname" . }}
  labels:
    {{- include "disconf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "disconf.selectorLabels" . | nindent 4 }}
