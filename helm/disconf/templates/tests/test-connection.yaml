apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "disconf.fullname" . }}-test-connection"
  labels:
    {{- include "disconf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "disconf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
