apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "clvpg.fullname" . }}-ingressroutebar
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`{{ .Release.Name }}.{{  .Values.global.root_domain }}`) && PathPrefix(`/clvpg`)
    kind: Rule
    services:
    - name: {{ include "clvpg.fullname" . }}-service
      port: {{ .Values.service.port }}
    middlewares:
    - name: {{ include "clvpg.fullname" . }}-middleware